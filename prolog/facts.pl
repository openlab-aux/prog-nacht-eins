how_many_prolog_developers_does_it_take_to_change_a_lightbulb. /* Because it needs a joke */

prolog_is_great. /* true. */

age(lukas,15).
age(prolog,44).

wtf(foo, 123, bla).
wtf(nein).
wtf('(:[])').

unicode("λαμβδα").
